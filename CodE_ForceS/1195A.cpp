#include<stdio.h>
#include<math.h>
int main()
{
    int n,i,k;
    scanf("%d %d",&n,&k);
    int arr[n+1];
    for (i=1;i,i<=n;i++)
    {
        scanf("%d",&arr[i]);
        if (arr[i]<=k && arr[i]>=1)
        {

        }
    }
}
